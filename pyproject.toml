[project]
name = "regex-parser"
version = "0.1.0"
description = "Web Application to visualize regex expression matching"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.1",
]
